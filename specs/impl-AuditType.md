# GENERATE: src/main/java/com/codrite/ruleaudit/audit/AuditType.java

Implement the AuditType enum EXACTLY as in the contract above (values MATCHED,
UNMATCHED, ERRORED). Package com.codrite.ruleaudit.audit. No extra members.
